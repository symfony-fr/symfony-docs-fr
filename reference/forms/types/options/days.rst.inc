days
~~~~

**type**: ``array`` **default**: 1 à 31

Liste des jours disponibles dans le type de champ ``day``. Cette option
n'est utile que dans le cas où l'option ``widget`` est définie à ``choice``::

    'days' => range(1,31)
