years
~~~~~

**type**: ``array`` **default**: cinq années précédentes aux cinq années suivant l'année en cours.

Liste des années disponibles dans le type de champ Year. Cette option n'est utile
que si l'option ``widget`` est à ``choice``.
